import { View, Text } from "react-native";

import styles from "./infocontainer.style";

const InfoContainer = ( ) => {
    return (
        <View style={styles.infoContainer} >
            <Text style={styles.infoText}>InfoContainer</Text>
        </View>
    )
}

export default InfoContainer;